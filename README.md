# Tanni Fashion House

A clean, fashion-only e-commerce storefront (Next.js + Prisma + PostgreSQL).
Cash on Delivery checkout, admin panel, order emails.

## Setup
1. `npm install`
2. Copy `.env.example` to `.env` and fill values
3. `npx prisma db push` then `npx prisma db seed`
4. `npm run dev`
